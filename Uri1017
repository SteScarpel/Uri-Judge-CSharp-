using System; 

class URI {

    static void Main(string[] args) { 

    int tempo = int.Parse(Console.ReadLine());
            int vm = int.Parse(Console.ReadLine());

            double distancia = (tempo * vm) / 12.0;

            Console.WriteLine(distancia.ToString("N3"));
    }

}
